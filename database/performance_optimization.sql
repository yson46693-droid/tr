-- ============================================
-- تحسينات الأداء لقاعدة البيانات
-- ============================================
-- هذا الملف يحتوي على فهارس إضافية لتحسين أداء الاستعلامات

-- تحسين فهارس جدول الإشعارات
-- إضافة فهرس مركب لتحسين استعلامات الإشعارات غير المقروءة
ALTER TABLE `notifications` 
ADD INDEX IF NOT EXISTS `idx_user_read_created` (`user_id`, `read`, `created_at`);

-- إضافة فهرس لتحسين استعلامات MAX(created_at)
ALTER TABLE `notifications` 
ADD INDEX IF NOT EXISTS `idx_user_created` (`user_id`, `created_at`);

-- ============================================
-- ملاحظات:
-- 1. هذه الفهارس تحسن أداء استعلامات الإشعارات بشكل كبير
-- 2. الفهرس المركب `idx_user_read_created` يحسن استعلامات الإشعارات غير المقروءة
-- 3. الفهرس `idx_user_created` يحسن استعلامات MAX(created_at) للمزامنة
-- ============================================


